{
  "name": "jeeves-weekly-reporter",
  "version": "1.0.0",
  "description": "Automated weekly product update report posted to Slack via Linear + Notion + Claude",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "@slack/web-api": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
